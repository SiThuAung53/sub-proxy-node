# Server
HOST=0.0.0.0
PORT=8080

# Upstream fetch
UPSTREAM_TIMEOUT=15
UPSTREAM_RETRIES=1

# Cache
CACHE_TTL_SECONDS=60
CACHE_STALE_TTL_SECONDS=240
CACHE_STALE_WHILE_REVALIDATE=1

# HTTP client pool
HTTP_MAX_CONNECTIONS=2000
HTTP_MAX_KEEPALIVE_CONNECTIONS=400

# Logs
LOG_UPSTREAM=0
