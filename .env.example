# Server
HOST=0.0.0.0
PORT=8080

# Upstream fetch
UPSTREAM_TIMEOUT=10
UPSTREAM_RETRIES=1

# Concurrency limits (per worker)
MAX_CONCURRENT_REQUESTS=200
MAX_QUEUE_SIZE=500

# HTTP client pool
HTTP_MAX_CONNECTIONS=2048
HTTP_MAX_KEEPALIVE_CONNECTIONS=512

# Logs
LOG_UPSTREAM=0
